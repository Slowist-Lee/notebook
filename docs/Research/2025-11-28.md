Single-Port/Capacity的效果是不是不太好

是不是切的不够细/分辨率（？）？目标比较小的情况下

---

导线交叉位置

补：识别文字，需要识别图上的字（数值、名字），yolo训，文本区域抓出来，小区域ocr(paddleocr_vl)

文字->匹配

![](Pasted%20image%2020251128195511.png)

欧姆->电阻

![](Pasted%20image%2020251128195905.png)

caption：网表而不是自然语言

向量

网表和电路图对齐（？）让大模型充分知道电路图

细粒度对齐->bbox信息

1张图+，其他-
有ocr之后，value是什么，-> 唯一正样本

电路图里有几个受控源，电阻？

电路图里1Ω的电阻

拉的过开

一版带，一版不带

数据集↑

---

子图对齐

loss 器件分类，正样本到底

---

开放性：

bbox->特征

需要有一个东西把bbox的特征弄出来，和文本算相似度

全局特征的那个位置对应特征

推理一次，拿到全局特征之后-bbox在哪-对应的特征

三个区域，变成一个特征，和文本对齐，怎么处理特征让效果最好

怎么表示导线的特征，和其他的东西怎么做池化？

和之前

算法，得到子图/区域的描述

，才能和文本对比学习

代码

---

clip

其他分辨率

FG-Clip2没啥区别

sigclip2分辨率？

搞两次-》可能是后面要做的事情

老师的建议是：标题写Clip可能不是很好，

---

插到大模型之后的事情也要创新：RL，连线

连线关系

强化学习框架很成熟，奖励函数之后可以训练

导线注意力的相似度，完整性

---

2. ocr 文字 - 手标，传统ocr之后调了

3. single-port 的效果不好，主要漏掉了一些

以loss来说还挺大的

细小的问题还没解决

有很多个模型/实际上只有一个模型

得到全局特征之后，剖一个特征区域之后对齐，模型到全局特征之后停了。这个时候来看bbox

子图：把电路图当成graph，graph是电路图的最好表示之一

连接关系；子图；图这个概念中

GNN->向量 更容易被LLM接受吗？
