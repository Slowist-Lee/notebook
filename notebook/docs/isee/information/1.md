# 信息的度量

## 一、事件的自信息

### 1. 定义

事件 ${X=x_k}$ 的自信息定义为 $I(x_k)=-\log P(A)$

$\log$函数的优势：
- 概率越小，信息量越大
- 非负数
- 可加性(置一个骰子，信息量是$\log_2 6$；置两个骰子，信息量即两个骰子的分别信息量相加：$\log_2 6$+$\log_2 6$，满足了可加性)
（至于底究竟是谁，其实和【单位】是一个性质的。底为2：bit；底为e：奈特）

确定性事件：信息量为0
不可能发生事件：信息量为无穷大

### 2. 性质

- 性质1：$q(x_k)$越大，$I_(x_k)$越小。即概率越小，自信息越大。
- 性质2：$q(x_k)=1$，$I(x_k)=0$。**确定事件**的自信息为0。
- 性质3：$q(x_k) \rightarrow 0$, $I(x_k) = \infty$
## 二、事件的条件自信息

### 1. 定义

事件$\{Y=y_j\}$发生的条件下，事件$\{X=x_k\}$的条件自信息定义为：
$$I(x_k|y_j)=-\log p(x_k|y_j)$$

### 2. 例子

## 三、事件的互信息

### 1. 定义：

$I(x_k;y_j)=I(x_k)-I(x_k|y_j)$



条件熵的意义：对变量$\mathrm{X}$可以进行多步分层的观察

但：必须是独立/树状的，不允许交叉

## 四、熵的性质

### 2. 凸性

- **凸性：**
- $H_k(P)$是
#### 2.1 凸函数介绍


![](Pasted%20image%2020250309102124.png)

非负凸集上的上凸函数取极大值的充要条件：

$$\frac{\partial f(\alpha)}{\partial \alpha_k}\begin{cases} = \lambda ,\forall \alpha_k>0 \\ < \lambda , \forall \alpha _k = 0\end{cases}$$

$\forall \alpha_k>0$: 概率$p_k$需为正值

推广到概率空间：（推导：拉格朗日乘数法）
（如何用计算机求解？）

## 三、随机变量间的平均互信息

2次都是面值，多大概率知道它是真币？10次、100次呢？

## 四、联合互信息

$I(x;y,z)=I(x)-I(x|y,z)$

## 五、相对熵

1. 定义：定义在相同字符表$\chi$上
$D(p//q)=\sum_x p(x)\log \frac{p(x)}{q(x)}$

下面我们要尝试用$D$来统一之前提到的$H$和$I$。



